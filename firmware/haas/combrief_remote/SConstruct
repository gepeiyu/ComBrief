#! /bin/env python

from aostools import Make

# Source and include paths are declared in package.yaml for this AliOS SDK.
# Kept here for HaaS Studio / source reviewers: app_state, protocol,
# ble_service, display, input, led, power.
defconfig = Make()

Export('defconfig')

defconfig.build_components()
